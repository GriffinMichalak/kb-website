type PhotosTabProps = {
    // articles: any;
    // publication: string;
    // setPublication: any;
    // options: string[];
};

export const PhotosTab = ({ }: PhotosTabProps) => {

    return (
        <div>
            Photos
        </div>
    );
};
